# PredictIQ — Testing & Demo
## Status: LOCKED

### Normal
Wokwi normal → ESP32 → API → DB → Dashboard → NORMAL.

### Degradation
Vibration rises → stored telemetry → health warning → prediction → explanation → alert.

### Critical
Multiple abnormal parameters → high/critical risk → alert → technician workflow.

### Offline
Heartbeat stops → timeout → OFFLINE.

### Maintenance
Alert → inspection → diagnosis → root cause → action → outcome → feedback → ground truth.

### Acceptance rule
The end-to-end demo must work without manually editing database records to fake the result.

### Honesty
Simulation supports development/demonstration. Industrial performance claims require real evidence.
